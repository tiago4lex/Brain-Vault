2025-11-10 10:20

Status: #developed #javascript 

Tags: [[JavaScript]] | [[JSON]]

----
# O que é JSON?

**JSON *(JavaScript Object Notation)*** é um formato leve de troca de dados, baseado em texto, usado para armazenar e transmitir informações entre sistemas.

Ele é fácil de ler e escrever por humanos, e fácil de interpretar e gerar por máquinas.

Apesar do nome conter "JavaScript", o JSON é independente de linguagem — ou seja, pode ser usado em Python, Java, C#, Node.js, entre outras linguagens.

![[sITOperations_Listing3_080416_mobile.png]]

---
# Conceito Fundamental

O JSON foi criado para representar dados estruturados de forma simples e padronizada, facilitando a comunicação entre cliente e servidor em sistemas web.

Por exemplo:

- O **backend** (API) envia dados em JSON.
- O **frontend** (JavaScript) lê e exibe esses dados.

---
# Estrutura e Sintaxe do JSON

A estrutura do JSON é baseada em pares chave-valor e em listas ordenadas de valores.

Ele utiliza apenas dois tipos de estruturas:

1. **Objeto JSON:** conjunto de pares `chave: valor` delimitado por `{}`.
2. **Array JSON:** lista ordenada de valores delimitada por `[]`.

---
# Exemplo completo de JSON

```json
{
  "nome": "Maria",
  "idade": 23,
  "estudante": true,
  "habilidades": ["JavaScript", "Python", "C++"],
  "endereco": {
    "cidade": "Curitiba",
    "estado": "PR"
  }
}
```

> Esse JSON representa uma pessoa com várias propriedades (chaves e valores).

---
# Regras de Sintaxe do JSON

O formato JSON segure **regras bem específicas:**

| Regra                     | Descrição                                                    | Exemplo                          |
| ------------------------- | ------------------------------------------------------------ | -------------------------------- |
| 1️⃣ **Objetos**           | Sempre entre **chaves `{}`**                                 | `{ "nome": "Maria" }`            |
| 2️⃣ **Arrays**            | Sempre entre **colchetes `[]`**                              | `["JS", "Python"]`               |
| 3️⃣ **Chaves (keys)**     | Devem estar **entre aspas duplas** (`"`)                     | `"idade": 23`                    |
| 4️⃣ **Valores**           | Podem ser **string, número, boolean, array, objeto ou null** | `"ativo": true`                  |
| 5️⃣ **Strings**           | Sempre com **aspas duplas** (`"`)                            | `"cidade": "São Paulo"`          |
| 6️⃣ **Sem vírgula final** | O último item **não leva vírgula**                           | ✅ `"idade": 23` ❌ `"idade": 23,` |
| 7️⃣ **Sem comentários**   | JSON **não permite comentários**                             | ❌ `// comentário`                |

---
# Tipos de Valores Aceitos no JSON

| Tipo        | Exemplo                  | Descrição                       |
| ----------- | ------------------------ | ------------------------------- |
| **String**  | `"Maria"`                | Texto entre aspas duplas        |
| **Número**  | `23`, `4.5`              | Inteiros ou decimais            |
| **Boolean** | `true`, `false`          | Verdadeiro ou falso             |
| **Array**   | `["HTML", "CSS", "JS"]`  | Lista ordenada                  |
| **Objeto**  | `{"cidade": "Curitiba"}` | Estrutura com pares chave-valor |
| **null**    | `null`                   | Valor nulo (sem dado)           |

---
# Diferença entre JSON e Objeto JavaScript

Apesar de parecerem parecidos, JSON e objetos JavaScript não são a mesma coisa.

| Aspecto      | Objeto JavaScript       | JSON                 |
| ------------ | ----------------------- | -------------------- |
| Formato      | Estrutura de dados      | Texto puro           |
| Aspas        | Aspas simples ou duplas | Apenas duplas        |
| Chaves       | Não precisam de aspas   | Sempre entre aspas   |
| Comentários  | Permitidos              | Não permitidos       |
| Tipo de dado | Dinâmico                | Estritamente textual |

### Exemplo

```js
// Objeto JavaScript
const pessoa = { nome: 'Maria', idade: 23 };

// JSON (string)
const pessoaJSON = '{"nome": "Maria", "idade": 23}';
```

---
# Conversão entre JSON e JavaScript

O JavaScript fornece duas funções globais para trabalhar com JSON:

## 1. `JSON.stringify()` → Objeto → JSON (texto)

Converte um **objeto JavaScript** em **string JSON**.

```js
const usuario = { nome: "Maria", idade: 23 };
const jsonString = JSON.stringify(usuario);

console.log(jsonString);
// Saída: '{"nome":"Maria","idade":23}'
```

> Usado, por exemplo, para enviar dados ao servidor via API.

## 2. `JSON.parse()` → JSON (texto) → Objeto

Converte uma **string JSON** em **objeto JavaScript**:

```js
const jsonString = '{"nome":"Maria","idade":23}';
const objeto = JSON.parse(jsonString);

console.log(objeto.nome); // Maria
```

> Usado, por exemplo, para **interpretar respostas JSON vindas de uma API**.

---
# JSON em APIs e Requisições

O formato JSON é o **padrão principal de comunicação entre cliente e servidor**.

## Exemplo prático

**Servidor envia:**

```json
{
  "id": 1,
  "produto": "Notebook",
  "preco": 3500.50
}
```

**Cliente (JavaScript) recebe:**

```js
fetch("https://api.exemplo.com/produto/1")
  .then(res => res.json())
  .then(dados => console.log(dados.produto));
```

**Saída:**

```mathematica
Notebook
```

> A função `res.json()` transforma a resposta do servidor (texto JSON) em **objeto JavaScript**.

---
# JSON Aninhado

Um JSON pode conter **outros objetoss e arrays dentro dele**, criando uma estrutura hierárquica.

```js
{
  "empresa": "TechCorp",
  "departamentos": [
    {
      "nome": "Desenvolvimento",
      "funcionarios": [
        { "nome": "Maria", "cargo": "Estagiário" },
        { "nome": "Alex", "cargo": "Engenheiro de Software" }
      ]
    }
  ]
}
```

> Esse formato é muito usado para representar **relacionamento entre entidades**.

---
# Usos Comuns do JSON

| Contexto                        | Uso                                                    |
| ------------------------------- | ------------------------------------------------------ |
| 🌍 **APIs REST**                | Envio e recebimento de dados entre cliente e servidor  |
| 💾 **Configurações**            | Arquivos `.json` (ex: `package.json` no Node.js)       |
| 📱 **Aplicativos Web e Mobile** | Comunicação com backend                                |
| 💻 **Banco de Dados NoSQL**     | MongoDB armazena documentos no formato JSON            |
| ⚙️ **Armazenamento Local**      | `localStorage.setItem('usuario', JSON.stringify(obj))` |

---
# Dicas Importantes

✅ JSON deve sempre ser **válido** (sem vírgulas extras, sem comentários, chaves com aspas duplas).  
✅ Ideal para **transmissão leve** de dados.  
✅ Sempre converter antes de enviar (`JSON.stringify`) e ao receber (`JSON.parse`).  
✅ Estrutura universal — pode ser usada em praticamente qualquer linguagem.

---
# Resumo

|Conceito|Descrição|
|---|---|
|**Significado**|JavaScript Object Notation|
|**Tipo**|Formato leve de troca de dados|
|**Base**|Texto estruturado|
|**Principais funções JS**|`JSON.stringify()` e `JSON.parse()`|
|**Usos**|APIs, configuração, armazenamento, integração|
|**Compatibilidade**|Multiplataforma (todas as linguagens modernas)|

---
# Conclusão

O **JSON** é uma das ferramentas mais importantes do ecossistema web moderno.  
Ele permite que **sistemas diferentes se comuniquem de forma simples, padronizada e eficiente**, sendo a **linguagem universal dos dados** na web.

Dominar JSON é essencial para qualquer desenvolvedor — especialmente quem trabalha com **JavaScript, APIs, Node.js, e aplicações web ou móveis**.