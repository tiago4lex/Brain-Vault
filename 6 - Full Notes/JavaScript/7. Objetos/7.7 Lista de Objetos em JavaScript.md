2025-10-27 10:22

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

E JavaScript, um **lista de objetos** é simplesmente um **array (lista)** cujos os elementos são objetos.

Esse tipo de estrutura é muito usado para representar **coleções de dados complexos**, como listas de usuários, produtos, alunos, filmes, etc.

---
# Conceito

Uma lista de objetos combina duas estruturas mais importantes do JavaScript:

- **Array** → coleção ordenado de elementos.
- **Objeto** → estrutura que armazena pares chave-valor.

Assim temos uma lista (`[]`) contendo vários objetos (`{}`).

Exemplo:

```js
const alunos = [
  { nome: "Lucas", idade: 23, curso: "Engenharia de Software" },
  { nome: "Maria", idade: 21, curso: "Cibersegurança" },
  { nome: "João", idade: 25, curso: "Ciência da Computação" }
];
```

Aqui, `alunos` é uma **lista com 3 objetos**, cada um representando um aluno com suas propriedades.

---
# Acessando Objetos em uma Lista

É possível acessar os objetos da lista pelo índice, assim como em qualquer array.

Exemplo:

```js
console.log(alunos[0]); // Primeiro Objeto
console.log(alunos[1].nome); // Acessa propriedade 'nome' do segundo objeto
```

**Saída:**

```js
{ nome: 'Lucas', idade: 23, curso: 'Engenharia de Software' }
Maria
```

Explicação:

- `alunos[0]` acessa o primeiro objeto da lista.
- `alunos[1].nome` acessa a **propriedade nome** do segundo objeto.

---
# Percorrendo uma Lista de Objetos

Para percorrer todos os objetos da lista, podemos usar **laços de repetição ou métodos de array**.

## Usando `for`

```js
for (let i = 0; i < alunos.length; i++) {
	console.log(alunos[i].nome);
}
```

**Saída:**

```
Lucas
Maria
João
```

## Usando `for...of`

```js
for (let aluno of alunos) {
	console.log(`${aluno.nome} - ${aluno.curso}`);
}
```

**Saída:**

```
Lucas - Engenharia de Software
Maria - Cibersegurança
João - Ciência da Computação
```

## Usando `forEach()`

```js
alunos.forEach(aluno => {
  console.log(`${aluno.nome} tem ${aluno.idade} anos.`);
});
```

**Saída:**

```
Lucas tem 23 anos.
Maria tem 21 anos.
João tem 25 anos.
```

**Explicação:**

O método `forEach()` executa uma função para **cada elemento da lista**, simplificando o código.

---
# Buscando e Filtrando Objetos

O JavaScript possui vários métodos poderosos para **buscar, filtrar e manipular** lisas de objetos.

## `find()` - Encontrar um único objeto

Retorna o **primeiro** objeto que atende à condição.

```js
const aluno = alunos.find(a => a.nome === "Maria");
console.log(aluno);
```

**Saída:**

```
{ nome: 'Maria', idade: 21, curso 'Cibersegurança' }
```

## `filter()` - Filtrar vários objetos

Retorna **todos os objetos** que satisfazem a condição.

```js
const maioresDe23 = alunos.filter(a => a.idade > 23);
console.log(maioresDe23);
```

**Saída:**

```
[{ nome: 'João', idade: 25, curso: 'Ciência da Computação' }]
```

## `map()` - Criar uma nota lista transformada

Transforma os objetos, criando uma **nova lista** com base na original.

```js
const nomes = alunos.map(a => a.nome);
console.log(nomes);
```

**Saída:**

```
['Lucas', 'Maria', 'João']
```

**Explicação:**

`map()` percorre todos os objetos e **retorna apenas os nomes**, criando um novo array.

## `reduce()` - Resumir informações

Usado para **calcular valores** (como médias, somas etc.).

```js
const mediaIdade = alunos.reduce((soma, aluno) => soma + aluno.idade, 0) / alunos.length;
console.log(mediaIdade);
```

**Saída:**

```
23
```

---
# Adicionando e Removendo Objetos da Lista

## Adicionar novo objeto com `push()`

```js
alunos.push({ nome: "Ana", idade, 20, curso: "Análise de Dados" });
console.log(alunos);
```

**Saída:**

```
[
  { nome: 'Lucas', idade: 23, curso: 'Engenharia de Software' },
  { nome: 'Maria', idade: 21, curso: 'Cibersegurança' },
  { nome: 'João', idade: 25, curso: 'Ciência da Computação' },
  { nome: 'Ana', idade: 20, curso: 'Análise de Dados' }
]
```

## Remover último objeto com `pop`

```js
alunos.pop();
console.log(alunos);
```

**Saída:**

```
[
  { nome: 'Lucas', idade: 23, curso: 'Engenharia de Software' },
  { nome: 'Maria', idade: 21, curso: 'Cibersegurança' },
  { nome: 'João', idade: 25, curso: 'Ciência da Computação' }
]
```

## Remover com `filter()`

```js
const semMaria = alunos.filter(a => a.nome !== "Maria");
console.log(semMaria);
```

**Saída:**

```
[
  { nome: 'Lucas', idade: 23, curso: 'Engenharia de Software' },
  { nome: 'João', idade: 25, curso: 'Ciência da Computação' }
]
```

**Explicação:**

Cria uma nova lista sem a Maria, sem alterar o array original.

---
# Atualizando Objetos na Lista

É possível atualizar um objetos específico acessando-o pelo índice ou usando métodos como `find()`.

**Exemplo:**

```js
const maria = alunos.find(a => a.nome === "Maria");
maria.idade = 22;

console.log(maria);
```

**Saída:**

```
{ nome: 'Maria', idade: 22, curso: 'Cibersegurança' }
```

**Dica:**

- Se for necessário **atualizar diretamente dentro do array** pode-se combinar `findIndex()` com atribuição:

```js
const indice = aluno.findIndex(a => a.nome === "João");
alunos[indice].curso = "Desenvolvimento Web";
```

---
# Lista de Objetos Aninhados

Os objetos dentro da lista podem **conter outros objetos**, criando estruturas complexas:

```js
const usuarios = [
  {
    nome: "Lucas",
    endereco: { cidade: "Curitiba", cep: "80000-000" }
  },
  {
    nome: "Maria",
    endereco: { cidade: "São Paulo", cep: "01000-000" }
  }
];

console.log(usuarios[0].endereco.cidade); // Curitiba
```

**Explicação:**

É comum trabalhar com listas de objetos aninhados ao lidar com APIs e JSONs.

---
# Ordenando Listas de Objetos

Podemos ordenar listas de objetos usando o método `sort()`:

```js
alunos.sort((a, b) => a.idade - b.idade);
console.log(aulnos);
```

**Saída:**

```
[
  { nome: 'Maria', idade: 21 },
  { nome: 'Lucas', idade: 23 },
  { nome: 'João', idade: 25 }
]
```

**Explicação:**

O `sort()` compara as idades e organiza a lista em ordem crescente.

---
# Resumo

| Ação           | Método     | Exemplo                                |
| -------------- | ---------- | -------------------------------------- |
| Acessar item   | `lista[i]` | `alunos[0].nome`                       |
| Adicionar item | `push()`   | `alunos.push(obj)`                     |
| Remover último | `pop()`    | `alunos.pop()`                         |
| Filtrar        | `filter()` | `alunos.filter(a => a.idade > 20)`     |
| Buscar         | `find()`   | `alunos.find(a => a.nome === "Lucas")` |
| Mapear         | `map()`    | `alunos.map(a => a.nome)`              |
| Reduzir        | `reduce()` | `alunos.reduce(...)`                   |
| Ordenar        | `sort()`   | `alunos.sort((a,b)=>a.idade-b.idade)`  |

---
# Boas Práticas

- ✅ Use **métodos imutáveis** (`map`, `filter`, `reduce`) sempre que possível.
- 🧱 Mantenha **chaves consistentes** entre os objetos da lista.
- ⚡ Evite alterar diretamente o array original — prefira criar **novas cópias**.
- 💬 Use **nomes descritivos** para facilitar a leitura (`usuarios`, `produtos`, `clientes`, etc).
- 🧩 Combine listas de objetos com **funções de callback** para processamento de dados.