2025-08-17 16:36

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que é um Array?

Um **array** é uma estrutura de dados que armazena uma **coleção ordenada de elementos**.

Um array pode ser definido como **uma lista ordenada de valores enumerados em que cada valor é chamado de _elemento_, ou _item_, e cada elemento se localiza numa posição fixa na lista chamada de _índice_.**

- Esses elementos podem ser de **qualquer tipo:** números, strings, objetos, funções, até outras arrays.
- Cada item do array é acessado através de um **índice numérico**, que começa em 0.

![[Pasted image 20250817171115.png]]

📌 Exemplo:

```js
let frutas = ["maçã", "banana", "laranja"];
console.log(frutas[0]); // "maçã"
console.log(frutas[2]); // "laranja"
```

---
# Criando Arrays

Existem algumas formas de criar arrays em JavaScript:

## 1. Usando colchetes `[]` (mais comuns)

```js
let numeros = [1, 2, 3, 4, 5];
```

## 2. Usando o construtor `Array`

```js
let letras = new Array("a", "b", "c");
let vazio = new Array(3); // Cria um array com 3 posições vazias
```

## 3. Arrays mistos

```js
let misto = [42, "texto", true, {nome: "João"}, [1, 2, 3]];
```

---
# Propriedade `.length`

A propriedade **length** retorna o tamanho (número de elementos) de um array.

Esta propriedade especifica o tamanho de um array, ou melhor dizendo, o **número de elementos**. É importante notar que não estamos falando aqui de índices ou posições, e sim da quantidade de valores (dados) contidos no array. Por exemplo:

```js
let cores = ["azul", "verde", "vermelho"];
console.log(core.length); // 3
```

---
# Acessando e Alterando Elementos

```js
let animais = ["gato", "cachorro", "coelho"];

console.log(animais[1]); // "cachorro"
animais[1] = "papagaio"; // alterando o valor
console.log(animais);    // ["gato", "papagaio", "coelho"]
```

---
# Principais Métodos de Arrays

## 1. Adição e Remoção de Elementos

- `push()` → adiciono no final
- `pop()` → remove do final
- `unshift()` → adiciona no início
- `shift()` → remove do início

```js
let numeros = [1, 2, 3];
numeros.push(4);   // [1, 2, 3, 4]
numeros.pop();     // [1, 2, 3]
numeros.unshift(0) // [0, 1, 2, 3]
numeros.shift();   // [1, 2, 3]
```

## 2. Métodos de Busca

- `indexOf(valor)` → retorna o índice do valor (ou -1 se não encontrar)
- `includes(valor)` → verifica se o valor existe no array

```js
let frutas = ["maçã", "banana", "uva"];
console.log(frutas.indexOf("banana")); // 1
console.log(frutas.includes("pera"));  // false
```

---
## 3. Iteração (percorrer arrays)

- `for` tradicional

```js
for (let i = 0; i < frutas.length; i++) {
	console.log(frutas[i]);
}
```

- `for...of`

```js
for (let fruta of frutas) {
	console.log(fruta);
}
```

- `forEach()`

```js
frutas.forEach(f => console.log(f));
```

## 4. Métodos de Transformação

- `map()` → cria um novo array transformando os elementos

```js
let numeros = [1, 2, 3];
let dobrados = numeros.map(n => n * 2);
console.log(dobrados); // [2, 4, 6]
```

- `filter()` → retornar apenas os elementos que atendem a uma condição

```js
let idades = [10, 18, 25, 15];
let maiores = idades.filter(idade => idade >= 18);
console.log(maiores); // [18, 25]
```

- `reduce()` → reduz o array a um único valor

```js
let valores = [10, 20, 30];
let soma = valores.reduce((acum, v) => acum + v, 0);
console.log(soma); // 60
```

## 5. Ordenação e Reversão

- `sort()` → ordena os elementos (como string por padrão)

```js
let nomes = ["Maria", "Ana", "Carlos"];
nomes.sort();
console.log(nomes); // ["Ana", "Carlos", "Maria"]
```

- `reverse()` → inverte a ordem

```js
nomes.reverse();
console.log(nomes); // ["Maria", "Carlos", "Ana"]
```

---
# Arrays Multidimensionais

Um array pode conter outros arrays, formando uma **matriz**.

```js
let matriz = [
	[1, 2, 3],
	[4, 5, 6],
	[7, 8, 9]
];

console.log(matriz[1][2]); // 6
```

---
# Diferença entre Array e Objeto

- **Objeto:** armazena pares chave-valor.
- **Array:** armazena valores em posições numeradas.

```js
let pessoa = {nome: "Maria", idade: 23}; // objeto
let notas = [8, 9, 10];                  // array
```

---
# Resumindo

- Arrays armazenam múltiplos valores em uma única variável.
- Podem conter qualquer tipo de dado.
- São indexados a partir do **0**.
- Oferecem muitos métodos úteis para manipulação de dados.

---
