
2025-08-17 17:42

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que é o método `slice()`?

O método `slice()` em JavaScript é usado para **copiar uma parte de um array ou string** e retornar um **novo array/string**, sem modificar o original.

Ele é muito útil quando queremos **pegar uma fatia** de dados sem alterar a coleção original.

---
# Sintaxe

```js
array.slice(inicio, fim);
string.slice(inicio, fim);
```

- `incio` → índice de onde a extração começa (inclusivo).
- `fim` → índice onde a extração termina (exclusivo, ou seja o índice informado não entra no resultado).
- Se o `fim` não for informado, a extração vai até o final.
- Se algum índice for **negativo**, ele conta a partir do final.

---
# Usando `slice()` em Arrays

## 1. Exemplo básico

```js
let frutas = ["maçã", "banana", "laranja", "uva", "manga"];
let parte = frutas.slice(1, 3);

console.log(parte);   // ["banana", "laranja"]
console.log(frutas);  // ["maçã", "banana", "laranja", "uva", "manga"] (orinal não muda)
```

## 2. Omitindo o parâmetro `fim`

```js
let numeros = [10, 20, 30, 40, 50];
let copia = numeros.slice(2);

console.log(copia); // [30, 40, 50]
```

## 3. Usando índices negativos

```js
let animais = ["gato", "cachorro", "coelho", "papagaio"];
let ultimos = animais.slice(-2);

console.log(ultimos); // ["coelho", "papagaio"]
```

---
# Usando `slice()` em Strings

O método também funciona com strings, retornando substrings.

```js
let frase = "JavaScript é incível!";
let parte = frase.slice(0, 10);

console.log(parte); // "JavaScript"
```

> Assim como nos arrays, ele **não altera a string original**

---
# Diferença entre `slice()` e `splice()`

É comum confundir os dois métodos:

- `slice()` → **não altera** o array original, apenas copia parte dele.
- `splice()` → **altera** o array original, podendo adicionar ou remover elementos.

📌 Exemplo:

```js
let numeros = [1, 2, 3, 4, 5];

// slice
let fatia = numeros.slice(1, 3);
console.log(fatia);   // [2, 3]
console.log(numeros); // [1, 2, 3, 4, 5] (inalterado)

// splice
let removidos = numeros.splice(1, 3);
console.log(removidos); // [2, 3, 4]
console.log(numeros);   // [1, 5] (modificado)
```

---
# Exemplos práticos de uso do `slice()`

## 1. Criar uma cópia do array

```js
let original = [1, 2, 3];
let copia = original.slice();

console.log(copia);  // [1, 2, 3]
```

## 2. Pegar os 3 primeiros elementos

```js
let letras = ["a", "b", "c", "d", "e"];
let primeiros = letras.slice(0, 3);

console.log(primeiros); // ["a", "b", "c"]
```

## 4. Usando com strings

```js
let palavra = "Programação";
console.log(palavra.slice(0, 7)); // "Program"
```

---
# Resumo

- `slice()` retorna uma **cópia parcial** de um array ou string.
- Ele **não altera** o original.
- Funciona tanto com índices positivos quanto negativos.
- Muito usado para **fatiar dados** ou **clonar arrays/strings**.

