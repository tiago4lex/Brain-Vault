
2025-08-18 11:32

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que são arrays de duas dimensões?

Um **array de duas dimensões** é basicamente um **array dentro de outro array**, formando uma **tabela ou matriz**.

- O **primeiro índice** representa a **linha**.
- O **segundo índice** representa a **coluna**.

📌 Em outras linguagens de programação (como C ou Java), chamamos de **matriz**.

---
# Criando arrays de Duas dimensões

## 1. Forma básica

```js
let matriz = [
	[1, 2, 3], // linha 0
	[4, 5, 6], // linha 1
	[7, 8, 9]  // linha 2
];
```

Neste exemplo:

- `matriz[0][0]` → `1`
- `matriz[1][2]` → `6`
- `matriz[2][1]` → `8`

## 2. Acessando Elementos da matriz

```js
console.log(matriz[0][1]); // 2 (linha 0, coluna 1)
console.log(matriz[2][0]); // 7 (linha 2, coluna 0)
```

## 3. Modificando elementos

Você pode atribuir novos valores diretamente:

```js
matriz [1][1] = 99;
console.log(matriz[1]); // [4, 99, 6]
```

---
# Percorrendo arrays de duas dimensões

## 1. Usando `for`

```js
for (let i = 0; i < matriz.length; i++) {
	for (let j = 0; j < matriz[i].length; j++) {
		console.log(`Elemento [${i}][${j}] = ${matriz[i][j]}`);
	}
}
```

## 2. Usando `for...of`

```js
for (let linha of matriz) {
	for (let valor of linha) {
		console.log(valor);
	}
}
```

---
# Criando matrizes dinamicamente

Você pode construir uma matriz usando laços:

```js
let linha = 3;
let colunas = 3;
let matrizDinamica = [0];

for (let i = 0; i < linhas; i++) {
	matrizDinamica[i] = [];
	for (let j = 0; j < colunas; j++) {
		matrizDinamica[i][j] = i + j; // exemplo simples
	}
}

console.log(matrizDinamica);

// [
//   [0, 1, 2],
//   [1, 2, 3],
//   [2, 3, 4]
// ]
```

---
# Exemplos práticos

## 1. Representando um tabuleiro de jogo

```js
let tabuleiro = [
	["X", "O", "X"],
	["0", "X", "O"],
	["X", " ", "O"]
];

console.log(tabuleiro[2][1]); // " " (célula vazia)
```

## 2. Calculando a soma de todos os elementos

```js
let soma = 0;
for (let i = 0; i < matriz.length; i++) {
	for (let j = 0; j < matriz[i].length; j++) {
		soma += matriz[i][j];
	}
}

console.log("Soma dos elementos:", soma);
```

## 3. Múltiplas Arrays

```js
const nomes = ["Ana", "Juliana", "Leonardo"];
const idades = [30, 35, 28];
const faculdade = [false, true, true];
 
const funcionarios = [nomes, idades, faculdade];
```

O array `funcionarios` é um array de duas dimensões. Há 3 arrays dentro dele, e para acessar os valores em `funcionarios` precisamos de 2 colchetes “`[][]`”. O primeiro colchete será usado para escolher qual dos 3 arrays dentro de `funcionarios` será acessado, podendo ser:

- 0 -> nomes
- 1 -> idades
- 2 -> faculdade

O segundo colchete será usado para acessar a informação dentro do array escolhido.

Outra forma de chamar os arrays de 2 dimensões é **matrizes**. Matrizes podem ser visualizadas como uma tabela do Excel, em que cada campo representa um elemento, e precisamos de uma linha e uma coluna para acessar um campo. Então, as linhas são o primeiro colchete e a coluna o segundo colchete.

---
# Boas práticas

✅ Sempre inicialize as matrizes antes de usar.  
✅ Lembre-se: nem todas as "linhas" precisam ter o mesmo número de elementos (arrays em JS são flexíveis).  
✅ Use `for...of` ou métodos funcionais (`map`, `flat`, etc.) para código mais legível.

---
# Resumo

- Arrays de duas dimensões = **arrays dentro de arrays**.
- São úteis para representar **tabelas, matrizes, grades e tabuleiros de jogos**.
- Podem ser acessados usando **dois índices** `[linha][coluna]`.
- Podem ser percorridos com laços **`for`**, **`for...of`** ou métodos de arrays.

