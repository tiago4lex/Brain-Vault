
2025-08-17 18:02

Status: #developed #javascript a

Tags: [[JavaScript]]

----
# Introdução

## O que é o método `splice()`?

O `splice()` é um método de arrays em JavaScript usado para **adicionar, remover ou substituir elementos em um array**.

> 🔑 Diferente do `slice()`, o `splice()` **modifica diretamente o array original**.

---
# Sintaxe

```js
arary.splice(inicio, deletCount, item1, item2, ...);
```

## Parâmetros

- `inicio` → índice a partir de onde a operação começa
- `deletCount` → número de elementos a serem removidos a partir do índice inicial.
	- Se for `0`, nenhum elemento será removido.
- `item1, item2, ...` → elementos que serão adicionados no lugar dos removidos (opcional).

> 👉 O método retorna um **novo array** contendo os elementos removidos.

---
# Exemplos de Uso

## 1. Removendo elementos

```js
let frutas = ["maçã", "banana", "laranja", "uva"];
let removidos = frutas.splice(1, 2);

console.log(frutas);    // ["maçã", "uva"] (array modificado)
console.log(removidos); // ["banana", "laranja"] (elementos removidos)
```

## 2. Adicionando elementos

```js
let animais = ["cachorro", "gato", "papagaio"];
animais.splice(1, 0, "coelho", "hamster");

console.log(animais);  // ["cachorro", "coelho", "hamster", "gato", "papagaio"]
```

> 📌 Aqui, removemos **0 elementos**, mas adicionamos dois no índice `1`.

## 3. Substituindo elementos

```js
let cores = ["vermelho", "azul", "verde"];
cores.splice(1, 1, "amarelo");

console.log(cores);  // ["vermelho", "amarelo", "verde"]
```

> 📌 Nesse caso, no índice `1`, o `"azul"` foi removido e substituído por `"amarelo"`.

## 4. Remover todos os elementos a partir de um índice

```js
let numeros = [1, 2, 3, 4, 5];
let removidos = numeros.splice(2);

console.log(numeros);    // [1, 2]
console.log(removidos);  // [3, 4, 5]
```

## 5. Usando índices negativos

```js
let letras = ["a", "b", "c", "d", "e"];
let removidos = letras.splice(-2, 1);

console.log(letras);    // ["a", "b", "c", "e"]
console.log(removidos); // ["d"]
```

> 📌 O índice negativo começa a contar de trás para frente (`-1` é o último elemento).

---
# Diferença entre `splice()` e `slice()`

|Característica|`slice()`|`splice()`|
|---|---|---|
|Modifica o array original|❌ Não|✅ Sim|
|Retorna|Um novo array com a cópia da parte selecionada|Um novo array com os elementos **removidos**|
|Pode adicionar elementos|❌ Não|✅ Sim|
|Índices negativos|✅ Funciona|✅ Funciona|

---
# Exemplos práticos de uso do `splice()`

## 1. Simular remoção de um item específico

```js
let tarefas = ["Estudar", "Treinar", "Ler", "Dormir"];
let indice = tarefas.indexOf("Ler");

if (indice !== -1) {
	tarefas.splice(indice, 1);
}

console.log(tarefas); // ["Estudar", "Treinar", "Dormir"]
```

## 2. Substituir vários elementos

```js
let lista = ["item1", "item2", "item3", "item4"];
lista.splice(1, 2, "novo1", "novo2", "novo3");

console.log(lista);  // ["item1", "novo1", "novo2", "novo3", "item4"]
```

## 3. Esvaziar um array

```js
let dados = [10, 20, 30, 40];
dados.splice(0, dados.length);

console.log(dados); // []
```

---
# Resumo

- **`splice()` altera o array original.**
- Pode ser usado para **remover**, **adicionar** ou **substituir** elementos.
- Retorna um novo array contendo os itens removidos.
- Aceita índices negativos.
- É muito flexível, mas deve ser usado com atenção, pois **muda o array de origem**.
