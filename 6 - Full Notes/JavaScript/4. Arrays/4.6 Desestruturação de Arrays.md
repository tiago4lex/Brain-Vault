2025-08-27 10:47

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que é a Desestruturação?

A desestruturação é um recurso introduzido no **ES6 *(ECMAScript 2015)*** que permita **extrair valores de arrays propriedades de objetos** e armazená-los em **variáveis** de forma simples e clara.

Sem desestruturação, normalmente acessaríamos os elementos assim:

```js
const numeros = [1, 2, 3];
const primeiro = numeros[0];
const segundo = numeros[1];

console.log(primeiro, segundo); // 1 2
```

Com **desestruturação:**

```js
const numeros = [1, 2, 3];
const [primeiro, segundo] = numeros;

console.log(primeiro, segundo); // 1 2
```

Ou seja, a desestruturação **pega valores do array e "desmonta"** eles em variáveis separadas.

---
# Como funciona na prática

## Exemplo simples

```js
const frutas = ["Maçã", "Banana", "Laranja"];
const [fruta1, fruta2] = frutas;

console.log(fruta1); // "Maçã"
console.log(fruta2); // "Banana"
```

> O JavaScript pega a posição `0` e `1` do array e atribui a variáveis automaticamente.

## Pulando elementos

É possível pular posições no array:

```js
const cores = ["Vermelho", "Verde", "Azul"];
const [ , segundaCor] = cores;

console.log(segundaCor); // "Verde"
```

## Definindo valores padrão

Se o array não tiver valores suficientes:

```js
const numeros = [10];
const [a, b = 20] = numeros;

console.log(a); // 10
console.log(b); // 20 (valor padrão)
```

## *Rest Operator* (`...`)

Você  pode capturar o **resto dos elementos:**

```js
const [primeiro, ...resto] = [1, 2, 3, 4];

console.log(primeiro); // 1
console.log(resto); // [2, 3, 4]
```

---
# Exemplo

```js
const alunos = ["João", "Juliana", "Caio", "Ana"];
const medias = [10, 8, 7.5, 9];

const lista = [alunos, medias];

function exibeNomeENota(aluno) {
	if (lista[0].includes(aluno)) {
		// desestruturação aqui 👇
		const [alunos, medias] = lista;

		const indice = alunos.indexOf(aluno);
		const mediaAluno = medias[indice];

		console.log(`${aluno} tem a média ${mediaAluno}`);
	} else {
		console.log("Estudante não existe na lista!");
	}
}

exibeNomeENota("Juliana"); // Juliana tem a média 8
exibeNomeENota("Maria");   // Estudante não existe na lista!
```

Existem duas listas:

```js
const alunos = ["João", "Juliana", "Caio", "Ana"];
const medias = [10, 8, 7.5, 9];

const lista = [alunos, medias];
```

O `lista` virou um **array de duas dimensões** (um array que contém dois arrays).  
Quando você precisa acessar `alunos` e `medias` novamente, poderia fazer assim:

```js
const alunos = lista[0];
const medias = lista[1];
```

Mas, usando **desestruturação**, fica muito mais **limpo e direto:**

```js
const [alunos, medias] = lista;
```

Ou seja, ele automaticamente pega:
- `lista[0]` → `alunos`
- `lista[1]` → `medias`

---
# Outros exemplos de desestruturação

## Trabalhando com funções

```js
function retornaPares() {
	return [2, 4, 6];
}

const [x, y, z] = retornaPares();
console.log(x, y, z); // 2 4 6
```

## Desestruturação aninhada (arrays dentro de arrays)

```js
const matriz = [[1, 2], [3, 4]];

const [[a, b], [c, d]] = matriz;
console.log(a, b, c, d); // 1 2 3 4
```

## Troca de variáveis com desestruturação

```js
let a = 10;
let b = 20;

[a, b] = [b, a];
console.log(a, b); // 20 10
```

---
# Conclusão

- A **desestruturação** facilita a extração de valores de arrays (e também de objetos).
- Permite escrever **código mais limpo e legível**.
- Funciona em arrays simples, arrays aninhados e até com valores padrão.
- No seu exemplo, ela simplifica o acesso a `alunos` e `medias` diretamente a partir da `lista`.

---
# Referências

- [Destructuring em JS](https://cursos.alura.com.br/extra/alura-mais/destructuring-em-js-c308)