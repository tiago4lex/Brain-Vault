2025-08-17 11:33

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que é uma *Arrow Function*?

A ***Arrow Function*** é uma forma mais curta e moderna de escrever funções em JavaScript, introduzido no **ES6 (ECMAScript 2015)**.

Ela usa a sintaxe ` =>` (seta) e é muita útil para simplificar funções anônimas e *callbacks*.

📌 Estrutura básica:

```js
const nomeFuncao = (parametros) => {
	// corpo da função
	return resultado;
};
```

---
# Diferenças para Funções Tradicionais

## 1. Sintaxe mais curta

- **Função tradicional:**

```js
function somar(a, b) {
	return a + b;
}
```

- **Arrow function:**

```js
const somar = (a, b) => a + b;
```

## 2. Retorno implícito

- Se a função tiver apenas **uma linha de código**, o `return` e as chaves `{}` podem ser omitidos.

```js
const dobrar = n => n * 2;

console.log(dobrar(5)); // 10
```

## 3. Não tem `this` próprio

- Arrow functions herdam o `this` do escopo onde foram criadas.
- Isso evita problemas comuns com funções tradicionais em objetos ou eventos.

Exemplo com função tradicional:

```js
function Pessoa() {
	this.nome = "Maria";
	setTimeout(function() {
		console.log(this.nome); // undefined
	}, 1000);
}

new Pessoa();
```

Exemplo com Arrow Function:

```js
function Pessoa() {
	this.nome = "Maria";
	setTimeout(() => {
		console.log(this.nome); // Maria
	}, 1000);
}

new Pessoa();
```

---
# Sintaxe da Arrow Function

## 1. Nenhum parâmetro

```js
const saudacao = () => console.log("Olá, mundo!");
saudacao(); // Olá, mundo!
```

## 2. Um parâmetro (parênteses opcionais)

```js
const dobrar = n => n * 2;
console.log(dobrar(4)); // 8
```

## 3. Múltiplos parâmetros (parênteses obrigatórios)

```js
const somar = (a, b) => a + b;
console.log(somar(3, 7)); // 10
```

## 4. Mais de uma instrução (uso de `{}` e `return`)

```js
const calcularArea = (largura, altura) => {
	const area = largura * altura;
	return area;
};

console.log(calcularArea(5, 3)); // 15
```

---
# Quando Usar Arrow Functions

✅ Ideal para **funções curtas e simples**  
✅ Muito usada em **callbacks**  
✅ Boa para **programação funcional** (map, filter, reduce)  
✅ Resolve problemas de escopo com `this`]

---
# Exemplos Práticos

## 1. Usando `map()`

```js
const numeros = [1, 2, 3, 4];
const dobrados = numeros.map(n => n * 2);

console.log(dobrados); // [2, 4, 6, 8]
```

## 2. Usando `filter()`

```js
const idades = [15, 22, 30, 12, 18];
const maioresDeIdade = idades.filter(idade => idade >= 18);

console.log(maioresDeIdade); // [22, 30, 18]
```

## 3. Usando `reduce()`

```js
const valores = [10, 20, 30];
const soma = valores.reduce((acumulados, valor) => acumulador + valor, 0);

console.log(soma); // 60
```

---
# Cuidados com Arrow Functions

⚠️ Não pode ser usada como **função construtora** (não suporta `new`).  
⚠️ Não tem `arguments` (mas pode usar o operador `...rest`).  
⚠️ Nem sempre é ideal para métodos de objetos, pois o `this` pode se referir ao objeto pai.

Exemplo:

```js
const pessoa = {
    nome: "Maria",
    falar: () => {
        console.log(this.nome); // ❌ undefined
    }
};
pessoa.falar();

```

---
# Resumindo

- Arrow functions oferecem **sintaxe curta e clara**.
- São ótimas para **funções anônimas** e **callbacks**.
- Não têm `this`, `arguments` ou `new`.
- Muito usadas em **programação funcional** (map, filter, reduce).