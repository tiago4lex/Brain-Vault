2025-08-16 18:50

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que são Funções?

Funções em JavaScript são **blocos de código reutilizáveis** que executam uma tarefa específica ou retornam um valor.

Elas ajudam a **organizar o código, evitar repetições e facilitar a manutenção.**

📌 Estrutura básica:

```js
function nomeDaFuncao(parametros) {
	// bloco de código
	// ...
	return valor; // opcional
}
```

## Parâmetros

Os parâmetros servem como *"inputs"* da função, é por onde uma função recebe os valores que precisa para executar corretamente.

Podem ser qualquer tipo de dado válido do JavaScript: números, strings, booleanos, arrays, objetos (os dois últimos vamos estudar nos próximos cursos);

É possível passal qualquer quantidade de parâmetros a uma função, separados por vírgula. Caso a função não precise receber nenhum parâmetro, declare apenas `()`;

Os parâmetros devem ser passados para a função no memento de execução, na ordem em que estão declarados.

**Exemplo:**

```js
function dividir(dividendo, divisor) {
	return dividendo / divisor;
}

const resulta = dividir(10, 2);
console.log(`O resultado é ${resultado}`); // O resultado é 5
```

Na chamada da função, o JavaScript atribui o primeiro parâmetro a `dividendo` e o segundo a `divisor`, conforme a ordem que está na declaração da função.

Se por algum motivo os valores de dividendo e divisor forem passados na ordem invertida no momento da **execução** da função, o resultado não será o mesmo:

```js
const resultdo = dividir(2, 10);
console.og(`O resultado é ${resultado}`); // O resultado é 0.2
```

---
# Tipos de Funções

## 1. Funções Declaradas

São funções criadas com a palavra-chave `function`.

```js
function saudacao() {
	console.log("Olá, seja bem-vindo!");
}

saudacao(); // Chamada da função
```

✅ Vantagem: podem se chamadas **antes ou depois** da sua declaração, graças ao *hoisting*.

## 2. Funções com Parâmetros

Podem receber valores (argumentos) para trabalhar dentro do seu escopo.

```js
function som(a, b){
	return a + b;
}

console.log(soma(5, 3)); // 8
```

## 3. Funções Anônimas

São funções **sem nome**, geralmente atribuídas a uma variável.

```js
const multiplicar = function(x, y) {
	return x * y;;
};

console.log(multiplica(4, 2)); // 8
```

## 4. Funções de Flecha *(Arrow Functions)*

Sintaxe mais curte introduzida no ES6.

```js
const dividir = (a, b) => {
	return a / b;
};

console.log(dividir(10, 2)); // 5
```

📌 Quando a função possui **apenas uma linha**, o `return` e as chaves `{}` podem ser omitidos:

```js
const dobrar = n => n * 2;
console.log(dobrar(7)); // 14
```

## 5. Funções como Valores

Em JavaScript, funções são **cidadãs de primeira classe**, ou seja, podem ser:

- Atribuídas a variáveis
- Passadas como argumento
- Retornadas por outras funções

```js
function executar(acao) {
	acao();
}

executar(() => console.log("Função passada como parâmetro!"));
```

---
# O comando `return`

O valor de retorno serve como *"output"* da função. Ele representa o resultado final obtido após a função executar o código dentro do bloco.

Para que a função retorno um valo, é utilizado a palavra-chave `return` seguida do valor que se deseja "retornar". Se nenhum valor for informado, a função por padrão retorna `undefined`.

A linha do retorno é **sempre a última linha do bloco**. Qualquer código escrito abaixo dessa linha se torna _unreachable_ (“inalcançável”) para o JavaScript.

Nem todas as funções retornam valores; dependendo do caso, uma função pode executar diversas instruções sem a necessidade de retornar nenhum valor no final.

O comando `return` é usado dentro de uma função para:

1. **Encerrar a execução da função**
2. **Retornar um valor para onde a função foi chamada**

📌 Exemplo:

```js
function soma(a, b) {
	return a + b; // retorna a soma
}

let resultado = soma(5, 10);
console.log(resultado); // 18
```

## 1. Encerramento da função

Tudo o que está após o `return` **não será executado**.

```js
function test() {
	console.lg("Antes do return");
	return "Resultado";
	console.log("Depois do return") // Nunca será executado
}

console.log(teste());
```

## 2. Função sem `return`

Se usarmos `return`, a função retorna `undefined` por padrão.

```js
function semReturn() {
	console.log("Não há retorno aqui");
}

let x = semReturn();
console.log(x); // undefined
```

---
# Exemplos práticos

## 1. Cálculo da média

```js
function calcularMedia(n1, n2, n3) {
	return (n1 + n2 + n3) / 3;
}

console.log(calcularMedia(8, 9, 7)); // 8
```

## Função de saudação personalizada

```js
function saudacao(nome) {
	return `Olá, ${nome}!`
}

console.log(saudacao("Maria")); // Olá, Maria!
```

---
# Resumo

- **Funções** organizam e reutilizam código.
- Podem ter **parâmetros** e **retornar valores**.
- `return` encerra a execução e envia um valor de volta.
- Se não houve `return`, a função retorn `undefined`.
