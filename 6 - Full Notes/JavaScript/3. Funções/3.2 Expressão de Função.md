2025-08-17 11:08

Status: #developed #javascript 

Tags: [[JavaScript]]

----
# Introdução

## O que é uma Expressão de Função?

Uma **expressão de função** é quando uma função é **criada e atribuída uma variável ou constante**.

Diferente de uma **declaração de função**, que usa apenas a palavra-chave `function`, na expressão a função é trada como um **valor**.

📌 **Estrutura básica**:

```js
const nomeVariavel = function(parametros) {
	// bloco de código
	return valor;
};
```

---
# Características Importantes

## 1. Função como Valor

Em JavaScript, funções são **cidadãs de primeira class *(first-calss citizens)***, ou seja, podem ser:

- Atribuídas a variáveis
- Passadas como parâmetros para outras funções
- Retornadas de funções

```js
const saudacao = function() {
	return "Olá!";
};

console.log(saudacau()); // Olá!
```

## 2. Diferença ente Declaração e Expressão de Função

- **Declaração de Função** *(Function Declaration)*

```js
function somar(a, b) {
	return a + b;
}

console.log(somar(2, 3)); // 5
```

✅ Pode ser chamada **antes** da definição, pois sofre *hoisting*.

- **Expressão ed Função** *(Function Expression)*

```js
const somar = function(a, b) {
	return a + b;
};

console.log(somar(2, 3)); // 5
```

⚠️ So pode ser chamada **depois** da atribuição, pois não sofre *hoisting*.

**Exemplo de erro:**

```js
console.log(subtrair(5, 2)); // ❌ ERRO: subtrair is not defined

const subtrair = function(a, b) {
	return a - b;
}
```

---
# Tipos de Expressões de Função

## 1. Expressão de Função Anônima

É quando a função **não tem nome e fica "guardada"** dentro da variável.

```js
const multiplicar = function(x, y) {
	return x * y;
};

console.log(multiplicar(4, 3)); // 12
```

## 2. Expressão de Função Nomeada

É quando damos um **nome à função dentro da expressão**.

Isso pode ajudar em depuração *(debugging)*.

```js
const dividir = function dividirNumeros(a, b) {
	return a / b;
};

console.log(dividir(10, 2)); // 5
```

📌 Mesmo nomeada, a função só pode ser chamada pelo **nome da variável** (`dividir`) e não pelo nome interno (`dividirNumeros`).

## 3. Função como Parâmetro *(Callback)*

Expressões de função são muito usadas como **funções de *callback***, passadas para outras funções.

```js
setTimout(function {
	console.log("Executado após 2 segundos!");
}, 2000);
```

## 4. Expressões de Função com *Arrow Functions*

As ***arrow functions*** são uma forma mais curta de escrever expressões de função.

```js
const dobrar = (n) => * 2;

console.log(dobrar(6)); // 12
```

---
# Vantagens da Expressão de Função

✅ Mais flexível: pode ser armazenada em variáveis ou objetos.  
✅ Permite criar **funções anônimas**.  
✅ Muito usada em callbacks e programação funcional.  
✅ Boa para **modularizar código** e criar funções rápidas.

---
# Exemplos Práticos

## 1. Expressão de função em Array

```js
cont operacoes = [
	function(a, b) { return a + b; },
	function(a, b) { return a - b; },
	function(a, b) { return a * b; }
];

console.log(operacoes[0](5, 3)); // 8
console.log(operacoes[1](5, 3)); // 2
console.log(operacoes[2](5, 3)); // 15
```

## 2. Expressão de função dentro de Objetos

```js
const calculadore = {
	somar: function(a, b) {
		return a + b;
	},
	multiplicar: function(a, b) {
		return a * b;
	}
};

console.log(calculadora.somar(4, 2));       // 6
console.log(calculadora.multiplicar(4, 2)); // 8
```

---
# Resumo

- **Expressão de Função** = função atribuída a uma variável ou constante.
- Diferente da **declaração**, não sofre _hoisting_.
- Pode ser **anônima** ou **nomeada**.
- Muito usada em **callbacks**, arrays e objetos.
- É a base para o uso de **arrow functions**.

