2025-08-07 12:13

Status: #developed 

Tags: [[CyberSecurity]]

----
# 🧪 O que vamos instalar?

## ✅ Aplicação vulnerável principal

- **DVWA - *Damn Vulnerable Wep Application*** (voltada para testes web, como SQLi, XSS, Brute Force...)

**(Opcional):**

- **Mutillidae** - Outra aplicação vulnerável em PHP
- **Vulnerable Web Services** (para testar APIs REST/XML)

---
# ⚙️ Etapas para instalar o DVWA no Ubuntu Server

## 1. Atualizar o sistema e instalar dependências

Execute no Ubuntu Server:

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install apache2 php php-mysqli mariadb-server git -y
```

## 2. Clonar DVWA no diretório do Apache

```bash
cd /var/www/html
sudo rm index.html #remove a página padrã
sudo git clone https://github.com/digininja/DVWA.git
```

## 4. Criar banco de dados e configurar senha

### 1. Acesse o MariaDB

```bash
sudo mysql
```

### 2. No prompt do MySQL

```sql
CREATE DATABASE dvwa;
GRANT ALL PRIVILEGES ON dvwa.* TO 'dvwauser'@'localhost' IDENTIFIED by 'senhaDVWA';
FLUSH PRIVILEGES;
EXIT;
```

### 3. Edite o arquivo de configuração do DVWA

```bash
sudo nano /var/www/html/DVWA/config/config.inc.php
```

Mude as seguintes linhas

```php
$_DVWA[ 'db_user' ] = 'dvwauser';
$_DVWA[ 'db_password' ] = 'senhaDVWA';
```

Salve com `Ctrl + 0`, `Enter` e `Ctrl + X`.

### 5. Reinicie o Apache

```bash
sudo systemctl restart apache2
```

### 6. Acessar o DVWA pela rede

Do seu Kali (WSL) ou navegador no Windows, acesse:

```arduino
http://192.168.56.10/DVWA/setup.php
```

Você verá a tela de setup do DVWA.

![[Pasted image 20250808145931.png]]

### 7. Configurar o DVWA no navegador

1. Clique  em **"Create/Reset Database"**
2. Vá em **DVWA Security > Low** (para começar com vulnerabilidades abertas)
3. Explore os módulos: SQL Injection, Command Injection, XSS, Login Brute Force, etc.

---
# ✅ Pronto


Você agora tem aplicação **intencionalmente vulnerável** pronta para:

- Exploração web com `sqlmap`, `nikto`, `burpsuite`, etc.
- Ataques de força bruta no login com `hydra`
- Análise de tráfego e logs

